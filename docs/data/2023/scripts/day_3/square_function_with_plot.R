# Square function

# this is x, a sequence
x <- -20:20

# x to the power of 2
y <- x ^ 2

# visualize x squared
plot(x,y)

# with more customization
plot(x,y, col="blue", pch="*", cex=2)
